'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _Button = require('./Button');

var _Button2 = _interopRequireDefault(_Button);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Button', function () {
  it('should render without throwing', function () {
    (0, _enzyme.shallow)(_react2.default.createElement(_Button2.default, null));
  });

  it('should add className to child', function () {
    var inherentClassName = 'rev-Button';
    var testClassName = '__TEST__';

    var childClassName = (0, _enzyme.shallow)(_react2.default.createElement(_Button2.default, { className: testClassName })).first().prop('className');

    expect(childClassName).to.contain(inherentClassName);
    expect(childClassName).to.contain(testClassName);
  });

  it('should handle props for different button types', function () {
    var button = (0, _enzyme.shallow)(_react2.default.createElement(_Button2.default, { secondary: true }));

    expect(button.first().prop('className')).to.contain('secondary');
  });

  it('should correctly disable a button', function () {
    var htmlButton = (0, _enzyme.mount)(_react2.default.createElement(_Button2.default, { disabled: true })).find('button').first();

    expect(htmlButton.prop('className')).to.contain('disabled');
    expect(htmlButton.prop('disabled')).to.equal(true);
  });
});
//# sourceMappingURL=Button.test.js.map