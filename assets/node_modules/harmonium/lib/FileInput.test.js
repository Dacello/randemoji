'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _FileInput = require('./FileInput');

var _FileInput2 = _interopRequireDefault(_FileInput);

var _sinon = require('sinon');

var _sinon2 = _interopRequireDefault(_sinon);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('FileInput', function () {
  it('should render without throwing', function () {
    (0, _enzyme.shallow)(_react2.default.createElement(_FileInput2.default, null));
  });

  it('should add className to child', function () {
    var testClassName = '__TEST__';

    var childClassName = (0, _enzyme.shallow)(_react2.default.createElement(_FileInput2.default, { className: testClassName })).first().prop('className');

    expect(childClassName).to.contain(testClassName);
  });

  it('can handle changes with onChange handler', function () {
    var spy = _sinon2.default.spy();
    var input = (0, _enzyme.mount)(_react2.default.createElement(_FileInput2.default, { onChange: spy }));

    input.find('input').simulate('change', {
      currentTarget: { value: 'C:\\fakepath\\snack-falcon.jpg' }
    });

    expect(spy.called).to.eq(true);
  });

  it('can handle changes with onChange handler', function () {
    var input = (0, _enzyme.mount)(_react2.default.createElement(_FileInput2.default, null));

    input.find('input').simulate('change', {
      currentTarget: { value: 'C:\\fakepath\\snack-falcon.jpg' }
    });
  });
});

describe('FileInput.Stack', function () {
  it('should render without throwing', function () {
    (0, _enzyme.shallow)(_react2.default.createElement(_FileInput2.default.Stack, null));
  });

  it('should add className to child', function () {
    var testClassName = '__TEST__';

    var childClassName = (0, _enzyme.shallow)(_react2.default.createElement(_FileInput2.default.Stack, { className: testClassName })).first().prop('className');

    expect(childClassName).to.contain(testClassName);
  });
});
//# sourceMappingURL=FileInput.test.js.map